version: '3'

services:
  db_postgres:
    container_name: ftp_grabber
    build: .
    env_file: .env
    volumes:
      - ./logs/ftp_grabber:/app/logs
      - ./screenshots:/app/screenshots
    ports:
      - "$FTP_PORT:$FTP_PORT"
    restart: always

